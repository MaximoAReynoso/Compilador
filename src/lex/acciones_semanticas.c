

void as_consume(int c, char *buffer, int *len) {
    // Esta accion semantica simplemente consume el caracter actual y no hace nada con el
    // Por lo tanto, no se necesita modificar el buffer ni la longitud
    // Se puede dejar el buffer y la longitud sin cambios
}